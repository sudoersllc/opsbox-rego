[project]
name = "aws-rego"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "boto3>=1.35.71",
    "loguru>=0.7.2",
]

[tool.uv]
dev-dependencies = [
    "moto>=5.0.22",
    "pytest>=8.3.4",
]
